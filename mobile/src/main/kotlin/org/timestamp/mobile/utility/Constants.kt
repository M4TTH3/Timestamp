package org.timestamp.mobile.utility

enum class EventFilterKey {
    ALL,
    TODAY,
    LATER,
    SEARCH // Custom filter based on a search
}

enum class Screen {
    Login,
    Home,
    Events,
    Calendar,
    Settings,
    CreateOrEditEvent,
    MapView
}